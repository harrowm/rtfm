# Project metadata and dependencies (uv-managed)
[project]
name = "rtfm-agent"
version = "0.1.0"
description = "AI documentation assistant with RAG, semantic caching, and memory"
requires-python = ">=3.12"
dependencies = []
